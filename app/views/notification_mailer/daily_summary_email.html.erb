<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .header { background-color: #F59E0B; color: white; padding: 20px; text-align: center; border-radius: 8px 8px 0 0; }
      .content { background-color: #f9fafb; padding: 30px; border: 1px solid #e5e7eb; border-top: none; }
      .summary-section { background-color: #ffffff; padding: 15px; margin: 15px 0; border-radius: 8px; }
      .summary-item { margin: 10px 0; padding: 8px 0; border-bottom: 1px solid #eee; }
      .highlight { font-weight: bold; color: #F59E0B; }
      .footer { text-align: center; padding: 20px; color: #6b7280; font-size: 14px; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Daily Summary - <%= Date.today.strftime('%B %d, %Y') %></h1>
      </div>
      
      <div class="content">
        <h2>Hello <%= @recipient.name %>!</h2>
        
        <p>Here's your daily summary for <%= Date.today.strftime('%B %d, %Y') %>:</p>
        
        <div class="summary-section">
          <h3>School Activities</h3>
          
          <% if @summary[:attendance_stats] %>
            <div class="summary-item">
              <span class="highlight">Attendance:</span> 
              <%= @summary[:attendance_stats][:present] %> students present, 
              <%= @summary[:attendance_stats][:absent] %> absent
            </div>
          <% end %>
          
          <% if @summary[:events_today] %>
            <div class="summary-item">
              <span class="highlight">Events Today:</span> <%= @summary[:events_today].count %> event(s)
            </div>
          <% end %>
          
          <% if @summary[:new_notifications] %>
            <div class="summary-item">
              <span class="highlight">New Notifications:</span> <%= @summary[:new_notifications].count %> message(s)
            </div>
          <% end %>
          
          <% if @summary[:fees_due] %>
            <div class="summary-item">
              <span class="highlight">Upcoming Fees:</span> <%= @summary[:fees_due].count %> fee(s) due soon
            </div>
          <% end %>
        </div>
        
        <p>Log in to your Kid Garden account to view detailed information about today's activities.</p>
        
        <p>Thank you for using Kid Garden!</p>
      </div>
      
      <div class="footer">
        <p>&copy; 2026 Kid Garden. All rights reserved.</p>
        <p>This is an automated message, please do not reply.</p>
      </div>
    </div>
  </body>
</html>